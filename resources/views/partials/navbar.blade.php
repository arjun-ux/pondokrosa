<header>
    <nav>
        <a href="{{ route('home') }}" class="logo">Roudlotussalam</a>
        <div class="menu-btn">
            <i class="fas fa-bars"></i>
        </div>
        <div class="nav-links">
            <a href="{{ route('home') }}#about">Tentang</a>
            <a href="{{ route('home') }}#education">Unit Pendidikan</a>
            <a href="{{ route('home') }}#programs">Program</a>
            <a href="{{ route('home') }}#facilities">Fasilitas</a>
            <a href="{{ route('blog') }}" class="active">Blog</a>
            <a href="{{ route('home') }}#contact">Kontak</a>
        </div>
    </nav>
</header>